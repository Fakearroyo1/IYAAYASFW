'use client';
import {useState} from 'react';
import {Field,NumberField,money} from './shared';
export function useCost(initialCost=0){const [packs,setPacks]=useState('1'),[units,setUnits]=useState('1'),[packCost,setPackCost]=useState(String(initialCost/100)),[freight,setFreight]=useState('0'),[purchaseTax,setPurchaseTax]=useState('0'),[discount,setDiscount]=useState('0');const quantity=Number(packs)*Number(units),total=Math.round((Number(packs)*Number(packCost)+Number(freight)+Number(purchaseTax)-Number(discount))*100),valid=Number.isSafeInteger(quantity)&&quantity>0&&Number(packs)>0&&Number(units)>0&&[packCost,freight,purchaseTax,discount].every(v=>Number.isFinite(Number(v))&&Number(v)>=0)&&total>=0;return{packs,setPacks,units,setUnits,packCost,setPackCost,freight,setFreight,purchaseTax,setPurchaseTax,discount,setDiscount,quantity,total,unitCost:valid?total/quantity:0,valid}}
export function CostInputs({cost}:{cost:ReturnType<typeof useCost>}){return <><div className="form-grid cost-grid"><NumberField label="Cases / packs" value={cost.packs} set={cost.setPacks} min={1} step="1"/><NumberField label="Units per pack" value={cost.units} set={cost.setUnits} min={1} step="1"/><NumberField label="Cost per pack ($)" value={cost.packCost} set={cost.setPackCost}/><NumberField label="Freight / delivery ($)" value={cost.freight} set={cost.setFreight}/><NumberField label="Purchase tax / fees ($)" value={cost.purchaseTax} set={cost.setPurchaseTax}/><NumberField label="Total discount ($)" value={cost.discount} set={cost.setDiscount}/></div><div className="calculation-summary"><span>{cost.quantity||0} individual units</span><strong>{cost.valid?money(cost.total):'Check quantities and costs'}</strong><span>{cost.valid?money(cost.unitCost)+' per unit':''}</span></div></>}
export function ReceiveFields(){const cost=useCost();return <><CostInputs cost={cost}/><input type="hidden" name="qty" value={cost.quantity}/><input type="hidden" name="amount" value={cost.total/100}/><Field label="Receipt / supplier reference" name="reference" required/><p className="fine">Allocate shared freight, tax, and discounts to this item only. Receiving updates stock and its weighted cost. Unknown prior cost stays unknown until all old stock is used.</p>{!cost.valid||cost.total<=0?<p className="notice warning">Enter a positive total cost and whole-unit quantity.</p>:null}</>}
