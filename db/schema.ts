import { sql } from 'drizzle-orm';
import { sqliteTable, text, integer, check, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const settings=sqliteTable('settings',{id:text('id').primaryKey(),enabled:integer('enabled').notNull().default(0),cashtag:text('cashtag').notNull().default(''),cashInstructions:text('cash_instructions').notNull().default('Place the exact amount in the snack bar cash box.'),reminderDays:integer('reminder_days').notNull().default(7)});
export const products=sqliteTable('products',{id:text('id').primaryKey(),name:text('name').notNull(),category:text('category').notNull(),detail:text('detail').notNull().default(''),image:text('image'),price:integer('price'),cost:integer('cost'),taxBp:integer('tax_bp'),stock:integer('stock').notNull().default(0),reorder:integer('reorder').notNull().default(5),active:integer('active').notNull().default(0),preorder:integer('preorder').notNull().default(0),version:integer('version').notNull().default(0),position:integer('position').notNull().default(0)},t=>[check('valid_stock',sql`${t.stock} >= 0`),check('valid_price',sql`${t.price} IS NULL OR ${t.price} > 0`)]);
export const members=sqliteTable('members',{id:text('id').primaryKey(),email:text('email').notNull().unique(),userId:text('user_id').unique(),name:text('name').notNull(),role:text('role').notNull().default('member'),active:integer('active').notNull().default(1),debt:integer('debt').notNull().default(0),credit:integer('credit').notNull().default(0),tabLimit:integer('tab_limit').notNull().default(2000),dueSince:integer('due_since')},t=>[check('valid_balances',sql`${t.debt} >= 0 AND ${t.credit} >= 0`)]);
export const orders=sqliteTable('orders',{id:text('id').primaryKey(),code:text('code').notNull().unique(),fingerprint:text('fingerprint').notNull(),memberId:text('member_id').references(()=>members.id),payer:text('payer').notNull(),method:text('method').notNull(),total:integer('total').notNull(),tax:integer('tax').notNull(),cost:integer('cost'),status:text('status').notNull(),createdAt:integer('created_at').notNull()},t=>[index('orders_member_created').on(t.memberId,t.createdAt)]);
export const orderItems=sqliteTable('order_items',{id:text('id').primaryKey(),orderId:text('order_id').notNull().references(()=>orders.id),productId:text('product_id').notNull().references(()=>products.id),name:text('name').notNull(),qty:integer('qty').notNull(),price:integer('price').notNull(),cost:integer('cost'),taxBp:integer('tax_bp').notNull(),preorder:integer('preorder').notNull().default(0)},t=>[index('items_order').on(t.orderId)]);
export const payments=sqliteTable('payments',{id:text('id').primaryKey(),fingerprint:text('fingerprint').notNull(),orderId:text('order_id').references(()=>orders.id),memberId:text('member_id').references(()=>members.id),purpose:text('purpose').notNull(),method:text('method').notNull(),amount:integer('amount').notNull(),status:text('status').notNull().default('pending'),reference:text('reference'),verifiedBy:text('verified_by'),createdAt:integer('created_at').notNull(),verifiedAt:integer('verified_at')},t=>[index('payments_status').on(t.status),index('payments_member').on(t.memberId),uniqueIndex('payment_provider_reference').on(t.method,t.reference)]);
export const audit=sqliteTable('audit',{id:text('id').primaryKey(),actor:text('actor').notNull(),kind:text('kind').notNull(),target:text('target').notNull(),detail:text('detail').notNull(),createdAt:integer('created_at').notNull()});
export const guards=sqliteTable('guards',{id:text('id').primaryKey(),valid:integer('valid').notNull()},t=>[check('operation_is_current',sql`${t.valid} = 1`)]);
export const expenses=sqliteTable('expenses',{id:text('id').primaryKey(),kind:text('kind').notNull(),amount:integer('amount').notNull(),description:text('description').notNull(),createdAt:integer('created_at').notNull()});
export const mutations=sqliteTable('mutations',{id:text('id').primaryKey(),fingerprint:text('fingerprint').notNull()});
